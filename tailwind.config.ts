import type { Config } from 'tailwindcss'

export default {
    content: ["./**/*.{html,js}"],
    darkMode: 'class',
    theme: {
        extend: {
            colors: {

            },
        },
    },
    plugins: [],
} satisfies Config